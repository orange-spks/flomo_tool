## 会话记录 [2026-05-09]

### 概要
修复 `htmlToMarkdown` 在提交到 Flomo 时的格式渲染问题，并调研硅基流动 API 配置。

### 已完成
- 修复 `htmlToMarkdown` 段落分隔逻辑（移除块元素尾部换行，顶层遍历插入 `\n\n`）
- 高亮/下划线 fallback 到标准 Markdown（Flomo 不支持 `==高亮==` 和 `<u>`）
- 调研硅基流动 API：完全兼容 OpenAI 格式，Base URL 为 `https://api.siliconflow.cn/v1`
- 更新 README.md、CLAUDE.md、MEMORY.md

### 待验证
- [ ] 富文本提交到 Flomo 后格式是否正确渲染（加粗、斜体、列表、换行）
- [ ] 硅基流动实际调用是否正常
